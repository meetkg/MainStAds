<div class="filters">
    <div class="search-filter">
        <input type="text" id="searchBox" placeholder="🔍 Search businesses..." />
    </div>

    <div class="category-filter">
        <select id="categoryDropdown">
            <option value="">Select Category</option>
        </select>
    </div>

    <div class="toggle-switch">
        <label for="showNearby" class="switch-label">Show Nearby Businesses:</label>
        <label class="switch">
            <input type="checkbox" id="showNearby">
            <span class="slider round"></span>
        </label>
    </div>
</div>

<style>
    .filters {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        margin-bottom: 1em;
        background-color: #fff;
        padding: 1em;
        border-radius: 4px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .search-filter, .category-filter, .toggle-switch {
        flex: 1;
        margin: 0.5em;
    }

    #searchBox, #categoryDropdown {
        width: 100%;
        padding: 8px 12px;
        border-radius: 4px;
        border: 1px solid #ccc;
        outline: none;
        transition: border-color 0.3s;
    }

        #searchBox:focus, #categoryDropdown:focus {
            border-color: #2196F3;
        }

    .toggle-switch {
        display: flex;
        align-items: center;
    }

    .switch-label {
        margin-right: 0.5em;
    }

    .switch {
        position: relative;
        display: inline-block;
        width: 60px;
        height: 34px;
        vertical-align: middle;
    }

        .switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

    .slider {
        position: absolute;
        cursor: pointer;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-color: #ccc;
        transition: background-color 0.4s;
    }

        .slider:before {
            position: absolute;
            content: "";
            height: 26px;
            width: 26px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: transform 0.4s;
        }

    input:checked + .slider {
        background-color: #2196F3;
    }

        input:checked + .slider:before {
            transform: translateX(26px);
        }

    .slider.round {
        border-radius: 34px;
    }

        .slider.round:before {
            border-radius: 50%;
        }
</style>
